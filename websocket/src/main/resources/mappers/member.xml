<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.emr.slgi.member.MemberDAO">
  <select id="getById">
    SELECT
      uuid,
      member_role as role,
      name,
      rrn,
      phone,
      create_date,
      update_date,
      delete_date
    FROM TB_MEMBER
    WHERE uuid = #{uuid}
  </select>
  
  <select id="getList">
    
    SELECT uuid, name, member_role as role
	FROM tb_member
	WHERE uuid != #{uuid}
  	AND (member_role = 'R002'
  	OR member_role = 'R003')

  </select>

  
  </mapper>